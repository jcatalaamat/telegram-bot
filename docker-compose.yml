services:
  telegram-bot:
    build: .
    container_name: telegram-transcription-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - MAX_AUDIO_SECONDS=${MAX_AUDIO_SECONDS:-600}
      - MAX_FILE_MB=${MAX_FILE_MB:-25}
      - TMP_DIR=/tmp/telegram_whisper_bot
    tmpfs:
      - /tmp/telegram_whisper_bot:size=512M
